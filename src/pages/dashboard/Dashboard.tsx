import { component$} from "@builder.io/qwik";
import { Link } from "@builder.io/qwik-city";


export default component$(() => {

  // Hopefully will get a nice dashboard going once server actions are here but until then
  // it's a pain to draw data from different sources
  return <><div>Coming soon!</div>
  <a href="/">Click here to return to your account</a>
  
  </>})